## Die Hard Dice

An app to create and manage dice sets for tabletop games.

This is all in browser app, no build.

### run a simple server

npx live-server

### types

check types of the project with
`npx tsc -p .`
